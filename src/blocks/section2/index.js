import block3 from './block3';  // Importar los bloques definidos

export default (editor, opts = {}) => {
  block3(editor, opts);  // Asegurarte de que está llamando la función del bloque correctamente
};
